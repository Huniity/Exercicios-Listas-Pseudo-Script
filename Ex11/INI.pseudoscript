#Exercício para declarar um array e define uma repetição do número 3

funcao start():
        print('Olá!')

array_5 = [1, 2, 3, 3, 4, 5]
tamanho = tamanho(array_5)

enquanto tamanho > 0 
    para cada elemento em lista(array)
        elemento == 3
            tamanho = tamanho - 1
    quebra()

# ou
# array_5 = [3] * 5

imprime("Bem vindo à este programa, este é o seu array: ", + array_5 + ".")